<div v-show='states.themesdisplay' class="ui sixteen wide column">
    <!-- <div class="ui form">
        <div class="inline fields">
            <label>Theme</label>
            <div class="field" data-tooltip="The people group that you want to label and analysis" data-position="top left" data-inverted="">
                <div class="ui mini compact menu">
                    <div class="ui simple dropdown item">
                        <span>{{ selections.themeName }}</span>
                        <i class="dropdown icon"></i>
                        <div class="menu">
                            <div v-for='item in settings.themes' class="item" v-on:click='changeTheme(item.name, item.value)'>{{ item.name }}</div>
                        </div>
                    </div>
                </div>
            </div>

            <label>Filter</label>
            <div class="field" data-tooltip="The time periods you used to filter out the top-N classes" data-position="top left" data-inverted="">
                <div class="ui mini compact menu">
                    <div class="ui simple dropdown item">
                        <span>{{ selections.modelParamName }}</span>
                        <i class="dropdown icon"></i>
                        <div class="menu">
                            <div v-for='item in settings.modelparams' class="item" :class="{'disabled': Number.parseInt(item.val) > 9}" v-on:click='changeThemeParam(item.name, item.val)'>{{ item.name }}</div>
                        </div>
                    </div>
                </div>
            </div>
                
        </div>
    </div> -->

    <!-- <div class="ui form">
        <div class="inline fields">
            <div class="field">

            </div>
            <label>Sort:</label>
            <div v-for="item in settings.sortorders" class="field">
                <div class="ui radio checkbox">
                    <input type="radio" name="sortorder" :value='item.val' v-model='selections.sortorderVal'>
                    <label>{{item.name}}</label>
                </div>
            </div>
        </div>
    </div> -->
</div>
<div v-show='labelbtndisplay' class="sixteen wide column">
    <!-- <div class="ui form">
        <div class="inline fields">
            <label>Top rate</label>
            <div class="field" data-tooltip="Percentage N, refers to top-N classes in whole cluster results" data-position="top left" data-inverted="">
                <input type="range" name="modelparamrange" min="0" max="100" v-model='selections.modelParamRangeVal'>
            </div>
            <label>{{ selections.modelParamRangeVal + '%' }}</label>
            <div class="field">
                <button id="labelBtn" class="ui mini secondary button" :class="{ 'loading': states.labeltrain }" v-on:click='labelTrain'>Label</button>
            </div>
        </div>
    </div> -->

    <!-- <input type="range" name="modelparamrange" min="0" max="100" v-model='selections.modelParamRangeVal'>
    <label>{{ selections.modelParamRangeVal + '%' }}</label>
    &nbsp;&nbsp;&nbsp;&nbsp;
    <button id="labelBtn" class="ui mini secondary button" :class="{ 'loading': states.labeltrain }" v-on:click='labelTrain'>Label</button> -->
</div>

<!-- <div v-show="states.anadisplay" class="sixteen wide column">
    <div class="ui form">
        <div class="inline fields">
            <label>Mode</label>
            <div v-for="item in settings.qmodes" class="field" :data-tooltip="item.tooltip" data-position="top left" data-inverted="">
                <div class="ui mini radio checkbox">
                    <input type="radio" name="recordqmode" :value="item.val" v-model="selections.vcqmodeVal">
                    <label>{{item.name}}</label>
                </div>
            </div>
        </div>
    </div>
</div> -->

<!-- Visual Analytics Mode -->
<!-- <div v-show="states.anadisplay" v-if='selections.vcqmodeVal === 0' class="sixteen wide column">
    <div class="ui mini compact menu">
        <div id="vcclaDropdown" class="ui simple disabled dropdown item">
            <span>{{ selections.vcclaName }}</span>
            <i class="dropdown icon"></i>
            <div class="menu">
                <div v-for='item in settings.classes' class="item" v-on:click='changeSelectCla(item)'>{{ item }}</div>
            </div>
        </div>
    </div>
    <div class="ui mini compact menu">
        <div class="ui simple dropdown item">
            <span>{{ selections.vctimeName }}</span>
            <i class="dropdown icon"></i>
            <div class="menu">
                <div v-for='item in settings.daytypes' class="item">
                    <i class="dropdown icon"></i>
                    <span>{{ item.name }}</span>
                    <div class="menu">
                        <div v-for="tpitem in item.timeperiods" class="item" v-on:click='changevcTime(item.name, tpitem.name, item.val, tpitem.val)'>{{ tpitem.name }}</div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <button id="claExpandBtn" class="ui mini secondary button" v-on:click='claExpandDisplay'>Expand</button>
    <button id="vcBtn" class="ui mini secondary button" :class="{ 'loading': states.vcquery }" v-on:click='vcQuery'>Query</button>
</div>
<div v-show="states.anadisplay" v-else-if='selections.vcqmodeVal === 1' class="sixteen wide column">
    <div class="vcPanelRow row">
        <div class="ui mini compact menu">
            <div id="vcclaDropdown" class="ui simple disabled dropdown item">
                <span>{{ selections.vcclaName }}</span>
                <i class="dropdown icon"></i>
                <div class="menu">
                    <div v-for='item in results.classlist' class="item" v-on:click='changeSelectCla(item)'>{{ item }}</div>
                </div>
            </div>
        </div>
        <div class="ui mini compact menu">
            <div id="compvcclaDropdown" class="ui simple disabled dropdown item">
                <span>{{ selections.compvcclaName }}</span>
                <i class="dropdown icon"></i>
                <div class="menu">
                    <div v-for='item in results.classlist' class="item" v-on:click='changeSelectCompCla(item)'>{{ item }}</div>
                </div>
            </div>
        </div>
    </div>
    <div class="vcPanelRow row">
        <div class="ui mini compact menu">
            <div class="ui simple dropdown item">
                <span>{{ selections.vctimeName }}</span>
                <i class="dropdown icon"></i>
                <div class="menu">
                    <div v-for='item in settings.daytypes' class="item">
                        <i class="dropdown icon"></i>
                        <span>{{ item.name }}</span>
                        <div class="menu">
                            <div v-for="tpitem in item.timeperiods" class="item" v-on:click='changevcTime(item.name, tpitem.name, item.val, tpitem.val)'>{{ tpitem.name }}</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <button id="claExpandBtn" class="ui mini secondary button" v-on:click='claExpandDisplay'>ClusterExpand</button>
        <button id="vcBtn" class="ui mini secondary button" :class="{ 'loading': states.vcquery }" v-on:click='vcQuery'>Query</button>
    </div>
</div>
<div v-show="states.anadisplay" v-else class="sixteen wide column">
    <div class="vcPanelRow row">
        <div class="ui mini compact menu">
            <div class="ui simple dropdown item">
                <span>{{ selections.vctimeName }}</span>
                <i class="dropdown icon"></i>
                <div class="menu">
                    <div v-for='item in settings.daytypes' class="item">
                        <i class="dropdown icon"></i>
                        <span>{{ item.name }}</span>
                        <div class="menu">
                            <div v-for="tpitem in item.timeperiods" class="item" v-on:click='changevcTime(item.name, tpitem.name, item.val, tpitem.val)'>{{ tpitem.name }}</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="ui mini compact menu">
            <div class="ui simple dropdown item">
                <span>{{ selections.compvctimeName }}</span>
                <i class="dropdown icon"></i>
                <div class="menu">
                    <div v-for='item in settings.daytypes' class="item">
                        <i class="dropdown icon"></i>
                        <span>{{ item.name }}</span>
                        <div class="menu">
                            <div v-for="tpitem in item.timeperiods" class="item" v-on:click='changecompvcTime(item.name, tpitem.name, item.val, tpitem.val)'>{{ tpitem.name }}</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="vcPanelRow row">
        <div class="ui mini compact menu">
            <div id="vcclaDropdown" class="ui simple disabled dropdown item">
                <span>{{ selections.vcclaName }}</span>
                <i class="dropdown icon"></i>
                <div class="menu">
                    <div v-for='item in settings.classes' class="item" v-on:click='changeSelectCla(item)'>{{ item }}</div>
                </div>
            </div>
        </div>
        
        <button id="claExpandBtn" class="ui mini secondary button" v-on:click='claExpandDisplay'>ClusterExpand</button>
        <button id="vcBtn" class="ui mini secondary button" :class="{ 'loading': states.vcquery }" v-on:click='vcQuery'>Query</button>
    </div>
</div> -->
<!-- Visual Analytics Mode -->

<!-- NEW VISUAL ANALYTICS LOGIC -->
<div v-show="states.anadisplay || true" class="sixteen wide column">
    <div class="ui form">
        <div class="inline fields">
            <button id="claExpandBtn" class="ui mini secondary button" v-on:click='claExpandDisplay'>Expand</button>

            <div class="ui mini compact menu">
                <div id="vcclaDropdown" class="ui simple dropdown item">
                    <span>{{ selections.vcclaName }}</span>
                    <i class="dropdown icon"></i>
                    <div class="menu">
                        <div v-for='item in settings.classes' class="item" v-on:click='changeSelectCla(item)'>{{ item }}</div>
                    </div>
                </div>
            </div>
            <div class="ui mini compact menu">
                <div class="ui simple dropdown item">
                    <span>{{ selections.vctimeName }}</span>
                    <i class="dropdown icon"></i>
                    <div class="menu">
                        <div v-for='item in settings.daytypes' class="item">
                            <i class="dropdown icon"></i>
                            <span>{{ item.name }}</span>
                            <div class="menu">
                                <div v-for="tpitem in item.timeperiods" class="item" v-on:click='changevcTime(item.name, tpitem.name, item.val, tpitem.val)'>{{ tpitem.name }}</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
           
            <button id="vcBtn" class="ui mini secondary button" :class="{ 'loading': states.vcquery }" v-on:click='vcQuery("basic")'>Display</button>
        </div>
    </div>
</div>

<div class="sixteen wide column">
    <div class="ui form">
        <div class="inline fields">
            <!-- add ITEM into comparison mode -->
            <div class="ui mini compact menu">
                <div class="ui simple dropdown item">
                    <span>{{ selections.vcqmodeName }}</span>
                    <i class="dropdown icon"></i>
                    <div class="menu">
                        <div v-for='item in settings.qmodes' class="item" v-on:click='changevcMode(item)'>{{item.name}}</div>
                    </div>
                </div>
            </div>

            <div v-show='selections.vcqmodeVal === 1' class="ui mini compact menu">
                <div id="compvcclaDropdown" class="ui simple dropdown item">
                    <span>{{ selections.compvcclaName }}</span>
                    <i class="dropdown icon"></i>
                    <div class="menu">
                        <div v-for='item in results.classlist' class="item" v-on:click='changeSelectCompCla(item)'>{{ item }}</div>
                    </div>
                </div>
            </div>
            <div v-show='selections.vcqmodeVal === 2' class="ui mini compact menu">
                <div class="ui simple dropdown item">
                    <span>{{ selections.compvctimeName }}</span>
                    <i class="dropdown icon"></i>
                    <div class="menu">
                        <div v-for='item in settings.daytypes' class="item">
                            <i class="dropdown icon"></i>
                            <span>{{ item.name }}</span>
                            <div class="menu">
                                <div v-for="tpitem in item.timeperiods" class="item" v-on:click='changecompvcTime(item.name, tpitem.name, item.val, tpitem.val)'>{{ tpitem.name }}</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <button v-show='selections.vcqmodeVal > 0' class="ui mini secondary button" :class="{ 'loading': states.vccompquery }" v-on:click='vcQuery("comp")'>Display</button>
        </div>
    </div>
</div>
<!-- NEW VISUAL ANALYTICS LOGIC -->
